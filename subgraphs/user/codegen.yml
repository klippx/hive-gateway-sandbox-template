schema: schema.gql
emitLegacyCommonJSImports: false
generates:
  ./generated/types.ts:
    plugins:
      - typescript
      - typescript-resolvers
    config:
      federation: true # generates types for reference resolvers
      typesPrefix: "$"
      enumsAsTypes: true
      useIndexSignature: true
      maybeValue: "T | undefined"
hooks:
  afterAllFileWrite:
    - "yarn prettier --write"
